<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof meta !== 'undefined' && meta.title ? meta.title : 'ITC Bot Dashboard' %></title>

    <!-- Open Graph Meta -->
    <meta property="og:title" content="<%= typeof meta !== 'undefined' && meta.title ? meta.title : 'ITC Bot Dashboard' %>" />
    <meta property="og:description" content="<%= typeof meta !== 'undefined' && meta.description ? meta.description : 'Manage and monitor your ITC Discord team bots, workshops, voice sessions, and team performance.' %>" />
    <meta property="og:type" content="<%= typeof meta !== 'undefined' && meta.type ? meta.type : 'website' %>" />
    <meta property="og:image" content="<%= typeof meta !== 'undefined' && meta.image ? meta.image : '/public/assets/logo.png' %>" />
    <meta property="og:image:alt" content="<%= typeof meta !== 'undefined' && meta.imageAlt ? meta.imageAlt : 'ITC Bot Dashboard Logo' %>" />
    <meta name="theme-color" content="<%= typeof meta !== 'undefined' && meta.themeColor ? meta.themeColor : '#e63946' %>" />

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary" />
    <meta name="twitter:title" content="<%= typeof meta !== 'undefined' && meta.title ? meta.title : 'ITC Bot Dashboard' %>" />
    <meta name="twitter:description" content="<%= typeof meta !== 'undefined' && meta.description ? meta.description : 'Manage and monitor your ITC Discord team bots.' %>" />

    <!-- Standard Meta -->
    <meta name="description" content="<%= typeof meta !== 'undefined' && meta.description ? meta.description : 'Manage and monitor your ITC Discord team bots, workshops, voice sessions, and team performance.' %>" />
    <meta name="keywords" content="<%= typeof meta !== 'undefined' && meta.keywords ? meta.keywords : 'ITC, Discord, Bot, Dashboard, Workshop, Teams, Management' %>" />

    <!-- Favicon -->
    <link rel="icon" type="image/png" href="/public/assets/logo.png" />

    <!-- External CSS -->
    <link rel="stylesheet" href="/public/css/dashboard.css" />
</head>
<body>
