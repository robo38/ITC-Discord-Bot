<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ITC Bot Dashboard</title>
    <style>
        :root {
            --bg-primary: #0a0a0e;
            --bg-secondary: rgba(18, 10, 12, 0.92);
            --bg-card: rgba(28, 12, 16, 0.55);
            --bg-input: rgba(12, 6, 8, 0.7);
            --text-primary: #f2e8ea;
            --text-secondary: #a08a8e;
            --accent: #e63946;
            --accent-hover: #c62d3a;
            --accent-glow: rgba(230, 57, 70, 0.25);
            --success: #4ade80;
            --danger: #ff6b6b;
            --warning: #fbbf24;
            --border: rgba(230, 57, 70, 0.15);
            --border-strong: rgba(230, 57, 70, 0.3);
            --radius: 14px;
            --glass-blur: 20px;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: var(--bg-primary);
            background-image:
                radial-gradient(ellipse at 20% 50%, rgba(230,57,70,0.06) 0%, transparent 60%),
                radial-gradient(ellipse at 80% 20%, rgba(230,57,70,0.04) 0%, transparent 50%);
            color: var(--text-primary);
            min-height: 100vh;
        }

        .navbar {
            background: rgba(18, 10, 12, 0.85);
            backdrop-filter: blur(var(--glass-blur));
            -webkit-backdrop-filter: blur(var(--glass-blur));
            padding: 14px 32px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            border-bottom: 1px solid var(--border);
            position: sticky;
            top: 0;
            z-index: 500;
        }

        .navbar-brand {
            font-size: 1.3rem;
            font-weight: 700;
            color: var(--accent);
            text-decoration: none;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .navbar-brand span { color: var(--text-primary); }

        .navbar-logo {
            width: 32px;
            height: 32px;
            border-radius: 8px;
            background: linear-gradient(135deg, var(--accent), #ff6b6b);
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 900;
            font-size: 0.7rem;
            color: #fff;
            letter-spacing: 0.5px;
        }

        .navbar-right {
            display: flex;
            align-items: center;
            gap: 14px;
        }

        .badge {
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 0.72rem;
            font-weight: 600;
            text-transform: uppercase;
            backdrop-filter: blur(8px);
        }

        .badge-dev { background: rgba(230,57,70,0.15); color: var(--accent); border: 1px solid rgba(230,57,70,0.3); }
        .badge-be { background: rgba(74,222,128,0.12); color: var(--success); border: 1px solid rgba(74,222,128,0.25); }
        .badge-admin { background: rgba(255,107,107,0.12); color: var(--danger); border: 1px solid rgba(255,107,107,0.25); }
        .badge-leader { background: rgba(251,191,36,0.12); color: var(--warning); border: 1px solid rgba(251,191,36,0.25); }

        .btn {
            padding: 8px 20px;
            border: none;
            border-radius: var(--radius);
            font-size: 0.88rem;
            font-weight: 600;
            cursor: pointer;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 6px;
            transition: all 0.25s ease;
        }

        .btn-primary {
            background: linear-gradient(135deg, var(--accent), #c62d3a);
            color: white;
            box-shadow: 0 2px 12px var(--accent-glow);
        }
        .btn-primary:hover { transform: translateY(-1px); box-shadow: 0 4px 20px var(--accent-glow); }
        .btn-danger { background: linear-gradient(135deg, #ff6b6b, #dc2626); color: white; }
        .btn-danger:hover { box-shadow: 0 4px 16px rgba(255,107,107,0.3); }
        .btn-success { background: linear-gradient(135deg, #4ade80, #16a34a); color: #0a0a0e; }
        .btn-success:hover { box-shadow: 0 4px 16px rgba(74,222,128,0.3); }
        .btn-outline {
            background: rgba(230,57,70,0.06);
            color: var(--text-secondary);
            border: 1px solid var(--border);
        }
        .btn-outline:hover { border-color: var(--accent); color: var(--accent); background: rgba(230,57,70,0.1); }
        .btn-sm { padding: 5px 14px; font-size: 0.8rem; }
        .btn-lg { padding: 12px 28px; font-size: 1rem; }

        /* Icon-only button */
        .btn-icon {
            width: 36px;
            height: 36px;
            padding: 0;
            border-radius: 10px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: rgba(230,57,70,0.06);
            border: 1px solid var(--border);
            color: var(--text-secondary);
            cursor: pointer;
            transition: all 0.2s;
            font-size: 0;
        }
        .btn-icon:hover { border-color: var(--accent); color: var(--accent); background: rgba(230,57,70,0.12); }
        .btn-icon svg { width: 18px; height: 18px; }
        .btn-icon.btn-icon-sm { width: 30px; height: 30px; }
        .btn-icon.btn-icon-sm svg { width: 15px; height: 15px; }        .btn-lg { padding: 12px 28px; font-size: 1rem; }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 32px 24px;
        }

        .page-header {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 28px;
        }

        .page-title {
            font-size: 1.5rem;
            font-weight: 700;
            background: linear-gradient(135deg, var(--text-primary), var(--accent));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .card {
            background: var(--bg-card);
            backdrop-filter: blur(var(--glass-blur));
            -webkit-backdrop-filter: blur(var(--glass-blur));
            border: 1px solid var(--border);
            border-radius: var(--radius);
            padding: 20px;
            margin-bottom: 16px;
            transition: transform 0.25s, box-shadow 0.25s, border-color 0.25s;
        }

        .card:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 32px rgba(230,57,70,0.08);
            border-color: var(--border-strong);
        }

        .card-header {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 12px;
        }

        .card-title {
            font-size: 1.1rem;
            font-weight: 600;
        }

        .card-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(340px, 1fr));
            gap: 16px;
        }

        .status-dot {
            width: 10px;
            height: 10px;
            border-radius: 50%;
            display: inline-block;
            margin-right: 6px;
        }

        .status-active { background: var(--success); box-shadow: 0 0 6px var(--success); }
        .status-inactive { background: var(--danger); box-shadow: 0 0 6px var(--danger); }
        .status-warning { background: #f59e0b; box-shadow: 0 0 6px #f59e0b; animation: pulse-warn 1.2s ease-in-out infinite; }
        @keyframes pulse-warn { 0%, 100% { opacity: 1; } 50% { opacity: 0.4; } }

        .info-row {
            display: flex;
            justify-content: space-between;
            padding: 6px 0;
            border-bottom: 1px solid rgba(255,255,255,0.05);
            font-size: 0.88rem;
        }

        .info-row:last-child { border-bottom: none; }
        .info-label { color: var(--text-secondary); }
        .info-value { color: var(--text-primary); font-weight: 500; }

        /* Forms */
        .form-group {
            margin-bottom: 20px;
        }

        .form-group label {
            display: block;
            margin-bottom: 6px;
            font-weight: 600;
            font-size: 0.9rem;
            color: var(--text-secondary);
        }

        .form-group input,
        .form-group select {
            width: 100%;
            padding: 10px 14px;
            background: var(--bg-input);
            border: 1px solid var(--border);
            border-radius: var(--radius);
            color: var(--text-primary);
            font-size: 0.9rem;
            transition: border-color 0.2s;
        }

        .form-group input:focus,
        .form-group select:focus {
            outline: none;
            border-color: var(--accent);
            box-shadow: 0 0 0 3px rgba(230, 57, 70, 0.12);
        }

        .form-row {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 16px;
        }

        .form-hint {
            font-size: 0.78rem;
            color: var(--text-secondary);
            margin-top: 4px;
        }

        .split-section {
            background: var(--bg-input);
            border: 1px dashed var(--border);
            border-radius: var(--radius);
            padding: 20px;
            margin-top: 12px;
        }

        .split-section h3 {
            font-size: 1rem;
            margin-bottom: 16px;
            color: var(--accent);
        }

        .alert {
            padding: 12px 16px;
            border-radius: var(--radius);
            margin-bottom: 20px;
            font-size: 0.9rem;
        }

        .alert-danger { background: rgba(255,107,107,0.1); border: 1px solid var(--danger); color: var(--danger); }
        .alert-success { background: rgba(74,222,128,0.1); border: 1px solid var(--success); color: var(--success); }

        /* Login page */
        .login-wrapper {
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            padding: 24px;
        }

        .login-card {
            background: var(--bg-card);
            backdrop-filter: blur(var(--glass-blur));
            -webkit-backdrop-filter: blur(var(--glass-blur));
            border: 1px solid var(--border-strong);
            border-radius: 16px;
            padding: 40px;
            width: 100%;
            max-width: 420px;
            box-shadow: 0 8px 48px rgba(230,57,70,0.08);
        }

        .login-title {
            font-size: 1.4rem;
            font-weight: 700;
            text-align: center;
            margin-bottom: 8px;
        }

        .login-subtitle {
            text-align: center;
            color: var(--text-secondary);
            margin-bottom: 28px;
            font-size: 0.9rem;
        }

        /* Toggle switch */
        .toggle-wrapper {
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .toggle {
            position: relative;
            width: 48px;
            height: 26px;
        }

        .toggle input { opacity: 0; width: 0; height: 0; }

        .toggle-slider {
            position: absolute;
            cursor: pointer;
            inset: 0;
            background: var(--border);
            border-radius: 26px;
            transition: 0.3s;
        }

        .toggle-slider:before {
            content: "";
            position: absolute;
            height: 20px;
            width: 20px;
            left: 3px;
            bottom: 3px;
            background: white;
            border-radius: 50%;
            transition: 0.3s;
        }

        .toggle input:checked + .toggle-slider { background: var(--accent); }
        .toggle input:checked + .toggle-slider:before { transform: translateX(22px); }

        /* Tables */
        table {
            width: 100%;
            border-collapse: collapse;
        }

        th, td {
            padding: 10px 14px;
            text-align: left;
            border-bottom: 1px solid var(--border);
            font-size: 0.88rem;
        }

        th {
            color: var(--text-secondary);
            font-weight: 600;
            font-size: 0.8rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .actions {
            display: flex;
            gap: 8px;
        }

        .empty-state {
            text-align: center;
            padding: 60px 24px;
            color: var(--text-secondary);
        }

        .empty-state h3 { margin-bottom: 8px; color: var(--text-primary); }

        /* Leader chips (index page) */
        .leader-chip {
            display: inline-flex;
            align-items: center;
            gap: 6px;
            background: var(--bg-input);
            padding: 4px 10px 4px 4px;
            border-radius: 20px;
            border: 1px solid var(--border);
        }

        /* Leader cards (detail page) */
        .leader-card {
            display: flex;
            align-items: center;
            gap: 10px;
            background: var(--bg-input);
            padding: 10px 16px;
            border-radius: var(--radius);
            border: 1px solid var(--border);
        }

        /* 3-dot dropdown */
        .dropdown-wrapper {
            position: relative;
            display: inline-block;
        }

        .dropdown-trigger {
            min-width: 32px;
            padding: 5px 8px !important;
            font-size: 1.1rem !important;
            line-height: 1;
            letter-spacing: 1px;
        }

        .dropdown-menu {
            display: none;
            position: absolute;
            right: 0;
            top: 100%;
            margin-top: 6px;
            background: rgba(18, 10, 12, 0.95);
            backdrop-filter: blur(24px);
            -webkit-backdrop-filter: blur(24px);
            border: 1px solid var(--border-strong);
            border-radius: var(--radius);
            min-width: 180px;
            box-shadow: 0 12px 40px rgba(0,0,0,0.5), 0 0 0 1px rgba(230,57,70,0.05);
            z-index: 100;
            overflow: hidden;
            animation: slideDown 0.15s ease;
        }

        @keyframes slideDown {
            from { opacity: 0; transform: translateY(-8px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .dropdown-wrapper.open .dropdown-menu { display: block; }

        .dropdown-item {
            display: block;
            padding: 10px 16px;
            color: var(--text-primary);
            text-decoration: none;
            font-size: 0.85rem;
            transition: background 0.15s;
        }

        .dropdown-item:hover { background: var(--bg-card); color: var(--accent); }

        /* Responsive */
        @media (max-width: 768px) {
            .form-row { grid-template-columns: 1fr; }
            .card-grid { grid-template-columns: 1fr; }
            .navbar { padding: 12px 16px; }
            .container { padding: 20px 16px; }
        }

        /* ─── Modal overlay ─────────────────────────────────────────── */
        .modal-overlay {
            display: none;
            position: fixed;
            inset: 0;
            background: rgba(0,0,0,0.7);
            backdrop-filter: blur(6px);
            z-index: 1000;
            align-items: center;
            justify-content: center;
        }
        .modal-overlay.active { display: flex; }
        .modal-content {
            background: rgba(18, 10, 12, 0.95);
            backdrop-filter: blur(var(--glass-blur));
            -webkit-backdrop-filter: blur(var(--glass-blur));
            border: 1px solid var(--border-strong);
            border-radius: 16px;
            width: 95%;
            max-width: 960px;
            max-height: 85vh;
            display: flex;
            flex-direction: column;
            box-shadow: 0 24px 64px rgba(0,0,0,0.5), 0 0 80px rgba(230,57,70,0.06);
            animation: modalIn 0.2s ease;
        }
        @keyframes modalIn {
            from { opacity: 0; transform: scale(0.96) translateY(10px); }
            to { opacity: 1; transform: scale(1) translateY(0); }
        }
        .modal-header {
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 18px 24px 14px;
            border-bottom: 1px solid var(--border);
        }
        .modal-header h3 { font-size: 1.05rem; color: var(--accent); }
        .modal-body {
            padding: 16px 24px;
            overflow-y: auto;
            flex: 1;
        }
        .modal-close {
            background: none;
            border: 1px solid var(--border);
            color: var(--text-secondary);
            width: 32px;
            height: 32px;
            border-radius: 50%;
            cursor: pointer;
            font-size: 1rem;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.2s;
        }
        .modal-close:hover { border-color: var(--danger); color: var(--danger); }

        /* ─── Search & filter bar ────────────────────────────────────── */
        .toolbar {
            display: flex;
            gap: 10px;
            margin-bottom: 14px;
            flex-wrap: wrap;
            align-items: center;
        }
        .toolbar input[type="text"] {
            padding: 8px 14px;
            background: var(--bg-input);
            border: 1px solid var(--border);
            border-radius: var(--radius);
            color: var(--text-primary);
            font-size: 0.85rem;
            width: 260px;
            transition: border-color 0.2s;
        }
        .toolbar input[type="text"]:focus { outline: none; border-color: var(--accent); }
        .toolbar select {
            padding: 8px 12px;
            background: var(--bg-input);
            border: 1px solid var(--border);
            border-radius: var(--radius);
            color: var(--text-primary);
            font-size: 0.82rem;
            cursor: pointer;
        }

        /* ─── Pagination ─────────────────────────────────────────────── */
        .pagination {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 4px;
            margin-top: 16px;
            user-select: none;
        }
        .pagination button {
            padding: 6px 12px;
            background: var(--bg-input);
            border: 1px solid var(--border);
            border-radius: 6px;
            color: var(--text-secondary);
            cursor: pointer;
            font-size: 0.82rem;
            transition: all 0.15s;
        }
        .pagination button:hover:not(:disabled) { border-color: var(--accent); color: var(--accent); }
        .pagination button.active { background: var(--accent); color: #fff; border-color: var(--accent); }
        .pagination button:disabled { opacity: 0.4; cursor: default; }

        /* ─── Copy to copy ──────────────────────────────────────────── */
        .copy-text {
            cursor: pointer;
            position: relative;
            transition: color 0.15s;
        }
        .copy-text:hover { color: var(--accent); }
        .copy-toast {
            position: fixed;
            bottom: 24px;
            left: 50%;
            transform: translateX(-50%);
            background: linear-gradient(135deg, var(--accent), #c62d3a);
            color: #fff;
            padding: 8px 20px;
            border-radius: 10px;
            font-size: 0.85rem;
            z-index: 2000;
            pointer-events: none;
            opacity: 0;
            transition: opacity 0.25s;
            box-shadow: 0 4px 20px var(--accent-glow);
        }
        .copy-toast.show { opacity: 1; }

        /* ─── Workshop start form ────────────────────────────────────── */
        .ws-form-row {
            display: flex;
            gap: 10px;
            align-items: flex-end;
            flex-wrap: wrap;
        }
        .ws-form-row .form-group { margin-bottom: 0; flex: 1; min-width: 120px; }

        /* ─── Overlapping Avatars Stack ─────────────────────────────── */
        .avatar-stack {
            display: flex;
            align-items: center;
        }
        .avatar-stack .avatar-item {
            position: relative;
            width: 34px;
            height: 34px;
            border-radius: 50%;
            border: 2px solid var(--bg-primary);
            overflow: hidden;
            margin-left: -10px;
            transition: transform 0.2s, z-index 0.2s;
            cursor: pointer;
            z-index: 1;
            flex-shrink: 0;
        }
        .avatar-stack .avatar-item:first-child { margin-left: 0; }
        .avatar-stack .avatar-item:hover { transform: translateY(-4px) scale(1.15); z-index: 10; }
        .avatar-stack .avatar-item img { width: 100%; height: 100%; object-fit: cover; }
        .avatar-stack .avatar-overflow {
            width: 34px;
            height: 34px;
            border-radius: 50%;
            background: var(--bg-input);
            border: 2px solid var(--border);
            margin-left: -10px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 0.7rem;
            color: var(--text-secondary);
            font-weight: 600;
            z-index: 0;
        }

        /* ─── Tooltip System ────────────────────────────────────────── */
        .tooltip-trigger { position: relative; }
        .tooltip-card {
            display: none;
            position: absolute;
            bottom: calc(100% + 10px);
            left: 50%;
            transform: translateX(-50%);
            background: rgba(18, 10, 12, 0.97);
            backdrop-filter: blur(24px);
            -webkit-backdrop-filter: blur(24px);
            border: 1px solid var(--border-strong);
            border-radius: 12px;
            padding: 14px 16px;
            min-width: 220px;
            max-width: 300px;
            box-shadow: 0 12px 40px rgba(0,0,0,0.6), 0 0 0 1px rgba(230,57,70,0.08);
            z-index: 600;
            pointer-events: none;
            animation: tooltipIn 0.15s ease;
        }
        .tooltip-trigger:hover .tooltip-card { display: block; }
        @keyframes tooltipIn {
            from { opacity: 0; transform: translateX(-50%) translateY(6px); }
            to { opacity: 1; transform: translateX(-50%) translateY(0); }
        }
        .tooltip-card::after {
            content: '';
            position: absolute;
            top: 100%;
            left: 50%;
            transform: translateX(-50%);
            border: 6px solid transparent;
            border-top-color: var(--border-strong);
        }
        .tooltip-card .tt-name { font-weight: 600; font-size: 0.9rem; color: var(--text-primary); margin-bottom: 2px; }
        .tooltip-card .tt-sub { font-size: 0.78rem; color: var(--text-secondary); margin-bottom: 8px; }
        .tooltip-card .tt-row {
            display: flex;
            justify-content: space-between;
            font-size: 0.8rem;
            padding: 3px 0;
            border-bottom: 1px solid rgba(255,255,255,0.04);
        }
        .tooltip-card .tt-row:last-child { border-bottom: none; }
        .tooltip-card .tt-label { color: var(--text-secondary); }
        .tooltip-card .tt-value { color: var(--text-primary); font-weight: 500; }
        .tooltip-card .tt-roles {
            display: flex;
            flex-wrap: wrap;
            gap: 4px;
            margin-top: 6px;
        }
        .tooltip-card .tt-role-tag {
            display: inline-block;
            padding: 2px 8px;
            background: rgba(230,57,70,0.1);
            border: 1px solid var(--border);
            border-radius: 10px;
            font-size: 0.7rem;
            color: var(--accent);
        }

        /* ─── Context Menu ──────────────────────────────────────────── */
        .ctx-menu {
            display: none;
            position: fixed;
            background: rgba(18, 10, 12, 0.97);
            backdrop-filter: blur(24px);
            -webkit-backdrop-filter: blur(24px);
            border: 1px solid var(--border-strong);
            border-radius: 12px;
            min-width: 200px;
            box-shadow: 0 12px 48px rgba(0,0,0,0.6), 0 0 0 1px rgba(230,57,70,0.08);
            z-index: 2000;
            overflow: hidden;
            animation: ctxIn 0.12s ease;
            padding: 6px 0;
        }
        .ctx-menu.open { display: block; }
        @keyframes ctxIn {
            from { opacity: 0; transform: scale(0.95); }
            to { opacity: 1; transform: scale(1); }
        }
        .ctx-menu-item {
            display: flex;
            align-items: center;
            gap: 10px;
            padding: 10px 16px;
            color: var(--text-primary);
            font-size: 0.85rem;
            cursor: pointer;
            transition: background 0.12s, color 0.12s;
            user-select: none;
        }
        .ctx-menu-item:hover { background: rgba(230,57,70,0.1); color: var(--accent); }
        .ctx-menu-item svg { width: 16px; height: 16px; flex-shrink: 0; opacity: 0.7; }
        .ctx-menu-item:hover svg { opacity: 1; }
        .ctx-menu-item.danger { color: var(--danger); }
        .ctx-menu-item.danger:hover { background: rgba(255,107,107,0.1); }
        .ctx-menu-divider { height: 1px; background: var(--border); margin: 4px 0; }

        /* ─── Role Name Tag ─────────────────────────────────────────── */
        .role-tag {
            display: inline-flex;
            align-items: center;
            gap: 4px;
            padding: 2px 10px;
            background: rgba(230,57,70,0.08);
            border: 1px solid var(--border);
            border-radius: 12px;
            font-size: 0.8rem;
            color: var(--text-primary);
            cursor: pointer;
            transition: all 0.15s;
            position: relative;
        }
        .role-tag:hover { border-color: var(--accent); background: rgba(230,57,70,0.15); }
        .role-tag .role-id-tip {
            display: none;
            position: absolute;
            bottom: calc(100% + 6px);
            left: 50%;
            transform: translateX(-50%);
            background: rgba(10,10,14,0.97);
            border: 1px solid var(--border-strong);
            border-radius: 8px;
            padding: 6px 10px;
            font-size: 0.75rem;
            color: var(--text-secondary);
            font-family: monospace;
            white-space: nowrap;
            z-index: 500;
        }
        .role-tag:hover .role-id-tip { display: block; }

        /* ─── Floating Action Button ────────────────────────────────── */
        .fab {
            position: fixed;
            bottom: 24px;
            right: 24px;
            width: 52px;
            height: 52px;
            border-radius: 50%;
            background: linear-gradient(135deg, var(--accent), #c62d3a);
            border: none;
            color: white;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            box-shadow: 0 4px 24px var(--accent-glow), 0 2px 8px rgba(0,0,0,0.4);
            z-index: 900;
            transition: transform 0.2s, box-shadow 0.2s;
        }
        .fab:hover { transform: translateY(-3px) scale(1.05); box-shadow: 0 8px 32px var(--accent-glow); }
        .fab svg { width: 24px; height: 24px; }

        /* ─── Video Modal ───────────────────────────────────────────── */
        .video-modal-content {
            max-width: 720px;
            padding: 0;
        }
        .video-modal-content .modal-header { padding: 14px 20px 10px; }
        .video-modal-content .modal-body { padding: 0 0 10px; }
        .video-modal-content video {
            width: 100%;
            border-radius: 0 0 14px 14px;
            max-height: 65vh;
            background: #000;
        }

        /* ─── Leaderboard Styles ────────────────────────────────────── */
        .lb-tabs {
            display: flex;
            gap: 4px;
            margin-bottom: 14px;
        }
        .lb-tab {
            padding: 6px 16px;
            background: var(--bg-input);
            border: 1px solid var(--border);
            border-radius: 8px;
            color: var(--text-secondary);
            font-size: 0.82rem;
            cursor: pointer;
            transition: all 0.15s;
        }
        .lb-tab:hover { border-color: var(--accent); color: var(--accent); }
        .lb-tab.active { background: var(--accent); color: #fff; border-color: var(--accent); }
        .lb-rank { font-weight: 700; font-size: 1rem; min-width: 30px; text-align: center; }
        .lb-rank.gold { color: #fbbf24; }
        .lb-rank.silver { color: #94a3b8; }
        .lb-rank.bronze { color: #d97706; }

        /* ─── Session Detail Modal ──────────────────────────────────── */
        .session-detail-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 12px;
            margin-bottom: 16px;
        }
        .session-detail-item {
            background: var(--bg-input);
            border-radius: 10px;
            padding: 12px 14px;
            border: 1px solid var(--border);
        }
        .session-detail-item .sd-label { font-size: 0.75rem; color: var(--text-secondary); text-transform: uppercase; letter-spacing: 0.5px; margin-bottom: 4px; }
        .session-detail-item .sd-value { font-size: 1.05rem; font-weight: 600; color: var(--text-primary); }

        /* ─── Workshop Modal (start workshop) ───────────────────────── */
        .ws-modal-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 14px;
        }
        .ws-modal-grid .form-group { margin-bottom: 0; }
        @media (max-width: 600px) { .ws-modal-grid { grid-template-columns: 1fr; } }

        /* ─── Enhanced Dropdown ──────────────────────────────────────── */
        .dropdown-menu {
            background: rgba(12, 8, 10, 0.98);
            backdrop-filter: blur(28px);
            -webkit-backdrop-filter: blur(28px);
            border: 1px solid var(--border-strong);
            border-radius: 12px;
            box-shadow: 0 16px 48px rgba(0,0,0,0.65), 0 0 0 1px rgba(230,57,70,0.06), inset 0 1px 0 rgba(255,255,255,0.03);
            padding: 6px 0;
        }
        .dropdown-item {
            display: flex;
            align-items: center;
            gap: 10px;
            padding: 10px 16px;
            font-size: 0.85rem;
            transition: all 0.15s;
        }
        .dropdown-item svg { width: 16px; height: 16px; opacity: 0.6; }
        .dropdown-item:hover { background: rgba(230,57,70,0.1); color: var(--accent); }
        .dropdown-item:hover svg { opacity: 1; }

        /* ─── Global Custom Scrollbar ────────────────────────────────── */
        ::-webkit-scrollbar {
            width: 8px;
            height: 8px;
        }
        ::-webkit-scrollbar-track {
            background: rgba(10, 10, 14, 0.5);
            border-radius: 10px;
        }
        ::-webkit-scrollbar-thumb {
            background: linear-gradient(180deg, var(--accent), #c62d3a);
            border-radius: 10px;
            border: 2px solid transparent;
            background-clip: padding-box;
        }
        ::-webkit-scrollbar-thumb:hover {
            background: linear-gradient(180deg, #ff4d5a, var(--accent));
            border-radius: 10px;
            border: 1px solid transparent;
            background-clip: padding-box;
        }
        ::-webkit-scrollbar-corner {
            background: transparent;
        }
        /* Firefox */
        * {
            scrollbar-width: thin;
            scrollbar-color: var(--accent) rgba(10, 10, 14, 0.5);
        }
        /* Specific scrollable containers */
        .modal-body, .modal-content, table, .dropdown-menu,
        .ctx-menu, pre, code, textarea {
            scrollbar-width: thin;
            scrollbar-color: var(--accent) rgba(10, 10, 14, 0.5);
        }

        /* ─── Snackbar / Toast Bar ───────────────────────────────────── */
        .snackbar {
            position: fixed;
            bottom: 32px;
            left: 50%;
            transform: translateX(-50%) translateY(100px);
            background: rgba(18, 10, 12, 0.97);
            backdrop-filter: blur(24px);
            -webkit-backdrop-filter: blur(24px);
            border: 1px solid var(--border-strong);
            border-radius: 14px;
            padding: 16px 24px;
            min-width: 300px;
            max-width: 480px;
            box-shadow: 0 12px 48px rgba(0,0,0,0.6), 0 0 0 1px rgba(230,57,70,0.08);
            z-index: 3000;
            opacity: 0;
            transition: transform 0.3s ease, opacity 0.3s ease;
            pointer-events: none;
        }
        .snackbar.show {
            opacity: 1;
            transform: translateX(-50%) translateY(0);
            pointer-events: auto;
        }
        .snackbar-title {
            font-size: 0.9rem;
            font-weight: 600;
            color: var(--text-primary);
            margin-bottom: 10px;
        }
        .snackbar-options {
            display: flex;
            gap: 8px;
            flex-wrap: wrap;
        }
        .snackbar-option {
            flex: 1;
            min-width: 120px;
            padding: 10px 16px;
            background: var(--bg-input);
            border: 1px solid var(--border);
            border-radius: 10px;
            color: var(--text-primary);
            cursor: pointer;
            text-align: center;
            font-size: 0.85rem;
            font-weight: 500;
            transition: all 0.15s;
        }
        .snackbar-option:hover {
            border-color: var(--accent);
            background: rgba(230,57,70,0.12);
            color: var(--accent);
        }
        .snackbar-option.selected {
            border-color: var(--accent);
            background: rgba(230,57,70,0.2);
            color: var(--accent);
            box-shadow: 0 0 12px var(--accent-glow);
        }
        .snackbar-close {
            position: absolute;
            top: 8px;
            right: 10px;
            background: none;
            border: none;
            color: var(--text-secondary);
            cursor: pointer;
            font-size: 1.1rem;
            padding: 4px;
            line-height: 1;
        }
        .snackbar-close:hover { color: var(--danger); }

        /* ─── Scheduled workshop countdown badge ─────────────────────── */
        .ws-scheduled-badge {
            display: inline-flex;
            align-items: center;
            gap: 6px;
            padding: 6px 14px;
            background: rgba(251,191,36,0.1);
            border: 1px solid rgba(251,191,36,0.3);
            border-radius: 10px;
            color: var(--warning);
            font-size: 0.85rem;
            font-weight: 600;
        }
        .ws-scheduled-badge .countdown {
            font-family: monospace;
            font-size: 0.9rem;
            min-width: 48px;
            text-align: center;
        }
    </style>
</head>
<body>
