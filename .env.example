# ╔══════════════════════════════════════════════════════════════════════╗
# ║                      ITCB BOT — ENVIRONMENT VARS                     ║
# ║             Copy this file to .env and fill in all values.           ║
# ╚══════════════════════════════════════════════════════════════════════╝

# ── Main Bot ─────────────────────────────────────────────────
# The main bot token (from Discord Developer Portal > Bot > Token)
TOKEN=your_main_bot_token_here

# The main bot's application/client ID (Developer Portal > General Information > Application ID)
CLIENT_ID=your_main_bot_client_id_here

# The main guild/server ID (Right-click server name > Copy Server ID)
GUILD_ID=your_guild_id_here

# Developer User ID — the ONLY person who can use /dev commands
# (Right-click your own profile > Copy User ID)
DEV_USER_ID=your_discord_user_id_here
DEV_GUILD_ID=
DEV_LOG=

# Back-end user ID - has admin-level access across all teams
BE_ID=Role_ID
ADMIN_ID=Role_ID

# ── MongoDB ──────────────────────────────────────────────────
# Connection string for your MongoDB database
# Local:   mongodb://localhost:27017/itcb
# Atlas:   mongodb+srv://user:pass@cluster.mongodb.net/itcb
MONGO_URI=mongodb://localhost:27017/itcb

# ── Google Sheets (for participant CSV sync) ─────────────────
SHEET_ID=your_google_sheet_id_here
GS_CLIENT_EMAIL=your_service_account_email@project.iam.gserviceaccount.com
GS_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\nyour_private_key_here\n-----END PRIVATE KEY-----\n

# ── Bootcamp Config ──────────────────────────────────────────
# The guild/server where the bootcamp runs
BOOTCAMP_GUILD_ID=your_bootcamp_guild_id_here

# Permanent invite code used to track new joins
BOOTCAMP_INVITE_CODE=your_invite_code_here

# Role given to all bootcamp participants
BOOTCAMP_ROLE=your_bootcamp_role_id_here

# Theme selection roles (assigned when a participant picks a theme)
THEME1_ROLE=your_theme1_role_id_here
THEME2_ROLE=your_theme2_role_id_here
THEME3_ROLE=your_theme3_role_id_here

# Leader role ID (optional fallback — leaders are also identified by LeaderID per team)
LEADER_ROLE_ID=your_leader_role_id_here

# Dashboard bots role (optional — used to restrict dashboard access to users with this role)
DASHBOARD_PORT=4000
DASHBOARD_SECRET=your-secret-key
DASHBOARD_URL=